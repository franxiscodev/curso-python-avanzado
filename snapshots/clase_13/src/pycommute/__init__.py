"""PyCommute — Asesor de movilidad inteligente.

# [CLASE 13] Estado final del proyecto — snapshot del curso completo.
# Este snapshot es identico a src/pycommute/ al cerrar la Clase 13.
# El proyecto completo incluye:
#   core/       — modelos y puertos (Protocol)
#   adapters/   — Weather, Route, Gemini, Ollama, Fallback, Cache
#   services/   — CommuteService
#   api/        — FastAPI: schemas, dependencies, routers, main
# Corriendo en: Docker Compose (api + ollama)
# CI/CD en:     GitHub Actions (lint + tests en cada push)
"""
__version__ = "0.1.0"
